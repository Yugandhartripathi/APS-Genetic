#include "dataStorage.hpp"

int main()
{
    //Let the TESTS Begin!
    int teamSize = -1;
    Gene person1;
    //Chromosome test(1,2);
    //Population testP;
    //Main Workflow
    //int teamSize, populationSize, gen;
    //cin >> teamSize >> populationSize >> gen;
    //Population p(1, populationSize, teamSize);
    //train(p, gen);
    Gene *x=readRecordAtIndexAndReturnGene(1);
    /*vector<Gene> genes(1);
    genes[0]=person1;
    test.setGeneAtIndex(0,genes[0]);
    cout<<test.getGeneAtIndex(0).getAptitude();
    bool rS[]={true,false,false,true,false,false,false};
    bool rD[]={true,false,true,false,false,false};
    */
    return 0;
}